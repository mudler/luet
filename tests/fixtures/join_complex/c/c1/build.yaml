requires:
  - name: "a"
    category: "test"
    version: ">=0"
  - name: "b"
    category: "test"
    version: ">=0"

requires_final_images: true

steps:
- | 
   /bin/sh -c "if [ -e /usr/bin/generate.sh ]; then ls -liah /usr/bin && exit 1; fi"